# 核心运行无第三方依赖（纯标准库即可跑）
# 以下为可选增强依赖（RAG / 本地模型 / Web界面）
# 未安装也能用纯规则引擎

# --- Web 界面（可选） ---
# streamlit>=1.30

# --- RAG / 向量检索（可选） ---
# chromadb>=0.4
# sentence-transformers>=2.2

# --- 数据处理（可选） ---
# pandas>=2.0
